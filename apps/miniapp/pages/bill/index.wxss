.bill-page {
  background: var(--color-bg-bill);
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  padding: 0 0 calc(740rpx + env(safe-area-inset-bottom));
}

.type-switch {
  display: flex;
  justify-content: center;
  height: 88rpx;
  background: #ffffff;
  border-bottom: 1rpx solid var(--color-divider);
}

.type-btn {
  position: relative;
  width: 200rpx;
  height: 88rpx;
  color: var(--color-text-sub);
  font-size: 30rpx;
  font-weight: 700;
  line-height: 88rpx;
  text-align: center;
}

.type-btn--active {
  color: var(--color-brand-deep);
}

.type-btn--income {
  color: var(--color-income);
}

.type-btn--active::after {
  content: "";
  position: absolute;
  left: 76rpx;
  right: 76rpx;
  bottom: 8rpx;
  height: 6rpx;
  border-radius: 999rpx;
  background: currentColor;
}

.amount-panel {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 40rpx;
  background: #ffffff;
  border-bottom: 1rpx solid var(--color-divider);
}

.amount-main {
  display: flex;
  align-items: baseline;
}

.amount-unit {
  margin-right: 10rpx;
  color: var(--color-text-sub);
  font-size: 36rpx;
  font-weight: 800;
}

.amount-value {
  color: var(--color-text-main);
  font-size: 72rpx;
  font-weight: 800;
  line-height: 1;
}

.amount-cursor {
  display: inline-block;
  width: 4rpx;
  height: 72rpx;
  margin-left: 4rpx;
  border-radius: 999rpx;
  background: var(--color-brand);
}

.selected-cat {
  display: flex;
  align-items: center;
  max-width: 240rpx;
  border-radius: 999rpx;
  color: var(--color-brand-deep);
  font-size: 36rpx;
  font-weight: 800;
}

.selected-cat-icon {
  width: 80rpx;
  height: 80rpx;
  margin-right: 16rpx;
  border-radius: 999rpx;
  background: var(--color-selected);
  text-align: center;
  line-height: 80rpx;
  font-size: 40rpx;
}

.selected-cat-name {
  max-width: 120rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.chev {
  margin-left: 8rpx;
  color: var(--color-text-sub);
}

.category-scroll {
  height: 320rpx;
  background: #ffffff;
}

.category-grid,
.section-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 8rpx;
  padding: 20rpx 32rpx 28rpx;
}

.category-item {
  height: 124rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border: 1rpx solid transparent;
  border-radius: 20rpx;
  color: var(--color-text-sub);
}

.category-item--active {
  border-color: var(--color-brand);
  background: var(--color-selected);
  color: var(--color-brand-deep);
  font-weight: 700;
}

.category-icon {
  font-size: 40rpx;
  line-height: 44rpx;
}

.category-name {
  max-width: 120rpx;
  margin-top: 8rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 22rpx;
}

.bill-bottom {
  position: fixed;
  left: 0;
  right: 0;
  bottom: calc(112rpx + env(safe-area-inset-bottom));
  z-index: 20;
  padding: 20rpx 32rpx 24rpx;
  background: #ffffff;
  border-top: 1rpx solid var(--color-divider);
}

.meta-row {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 12rpx;
  margin-bottom: 22rpx;
}

.meta-pill {
  min-width: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 60rpx;
  padding: 0 20rpx;
  border-radius: 20rpx;
  background: var(--color-meta);
  color: var(--color-text-sub);
}

.meta-text {
  max-width: 86rpx;
  margin-left: 6rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.keyboard {
  display: flex;
  gap: 16rpx;
}

.keyboard-left {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.key-row {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
}

.keyboard-right {
  width: 144rpx;
  height: 464rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.key {
  height: 104rpx;
  border-radius: 16rpx;
  background: var(--color-surface-soft);
  text-align: center;
  line-height: 104rpx;
  font-size: 46rpx;
  font-weight: 700;
}

.key--delete {
  background: var(--color-warning-soft);
  color: var(--color-warning);
}

.key--done {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: var(--color-brand);
  color: #ffffff;
  line-height: 1.2;
  font-size: 28rpx;
}

.done-mark {
  margin-bottom: 12rpx;
  font-size: 52rpx;
}

.sheet-mask {
  position: fixed;
  inset: 0;
  z-index: 5000;
  display: flex;
  align-items: flex-end;
  background: rgba(43, 74, 58, 0.45);
}

.sheet {
  position: relative;
  width: 100%;
  max-height: calc(86vh - env(safe-area-inset-bottom));
  padding: 36rpx 32rpx calc(32rpx + env(safe-area-inset-bottom));
  border-radius: 40rpx 40rpx 0 0;
  background: #ffffff;
}

.sheet::before {
  content: "";
  position: absolute;
  top: 16rpx;
  left: 50%;
  width: 80rpx;
  height: 8rpx;
  margin-left: -40rpx;
  border-radius: 999rpx;
  background: var(--color-divider);
}

.sheet--voice {
  max-height: 82vh;
}

.sheet-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
}

.sheet-title {
  font-size: 36rpx;
  font-weight: 800;
}

.sheet-close {
  width: 64rpx;
  height: 64rpx;
  text-align: center;
  line-height: 58rpx;
  color: var(--color-text-sub);
  font-size: 48rpx;
}

.sheet-scroll {
  max-height: 760rpx;
}

.sheet-scroll--small {
  max-height: 520rpx;
}

.section-title {
  margin: 18rpx 4rpx 8rpx;
  color: var(--color-text-sub);
  font-weight: 700;
}

.sheet-footer {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
  margin-top: 24rpx;
}

.account-item {
  display: flex;
  align-items: center;
  min-height: 108rpx;
  padding: 18rpx 20rpx;
  border-radius: 24rpx;
  margin-bottom: 12rpx;
}

.account-item--active {
  background: var(--color-selected);
}

.account-icon {
  width: 72rpx;
  height: 72rpx;
  margin-right: 18rpx;
  border-radius: 24rpx;
  background: #ffffff;
  text-align: center;
  line-height: 72rpx;
}

.account-main {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.account-name {
  font-weight: 700;
}

.account-sub {
  margin-top: 4rpx;
  color: var(--color-text-sub);
  font-size: 22rpx;
}

.check {
  color: var(--color-brand);
  font-size: 34rpx;
}

.tag-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 14rpx;
}

.tag-item {
  padding: 14rpx 22rpx;
  border-radius: 999rpx;
  background: var(--color-surface-soft);
  color: var(--color-text-sub);
}

.tag-item--active {
  background: var(--color-selected);
  color: var(--color-brand-deep);
  font-weight: 700;
}

.date-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 72rpx;
  font-size: 32rpx;
  font-weight: 800;
}

.date-head text:first-child,
.date-head text:last-child {
  width: 72rpx;
  text-align: center;
  color: var(--color-brand);
  font-size: 48rpx;
}

.date-week,
.date-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  text-align: center;
}

.date-week {
  margin-top: 12rpx;
  color: var(--color-text-sub);
  font-size: 22rpx;
}

.date-day {
  height: 72rpx;
  margin: 6rpx;
  border-radius: 18rpx;
  line-height: 72rpx;
}

.date-day--selected {
  background: var(--color-brand);
  color: #ffffff;
  font-weight: 800;
}

.voice-list {
  max-height: 760rpx;
}

.voice-item {
  position: relative;
  padding: 22rpx 72rpx 22rpx 22rpx;
  margin-bottom: 14rpx;
  border: 1rpx solid var(--color-divider);
  border-radius: 24rpx;
}

.voice-row {
  display: flex;
  justify-content: space-between;
  font-weight: 800;
}

.voice-amount {
  color: var(--color-brand-deep);
}

.voice-amount--income {
  color: var(--color-income);
}

.voice-remark,
.voice-warn {
  display: block;
  margin-top: 8rpx;
  color: var(--color-text-sub);
  font-size: 24rpx;
}

.voice-warn {
  color: var(--color-warning);
}

.voice-delete {
  position: absolute;
  right: 20rpx;
  top: 20rpx;
  width: 44rpx;
  height: 44rpx;
  color: var(--color-error);
  text-align: center;
  line-height: 40rpx;
  font-size: 40rpx;
}

.recording-panel {
  position: fixed;
  left: 0;
  right: 0;
  bottom: calc(112rpx + env(safe-area-inset-bottom));
  z-index: 30;
  display: flex;
  flex-direction: column;
  gap: 28rpx;
  min-height: 264rpx;
  padding: 32rpx 36rpx;
  border: 1rpx solid var(--color-divider);
  border-radius: 40rpx 40rpx 0 0;
  background: #ffffff;
}

.wave-box {
  height: 144rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  border-radius: 32rpx;
  background: var(--color-meta);
}

.wave-bar {
  width: 10rpx;
  border-radius: 999rpx;
  background: rgba(93, 190, 136, 0.44);
}

.wave-bar--1 {
  height: 32rpx;
}

.wave-bar--2 {
  height: 52rpx;
  background: rgba(93, 190, 136, 0.66);
}

.wave-bar--3 {
  height: 76rpx;
  background: var(--color-brand);
}

.wave-bar--4 {
  height: 100rpx;
  background: var(--color-brand-deep);
}

.wave-bar--5 {
  height: 60rpx;
  background: rgba(93, 190, 136, 0.66);
}

.wave-bar--6 {
  height: 40rpx;
}

.wave-bar--7 {
  height: 88rpx;
  background: var(--color-brand);
}

.wave-bar--8 {
  height: 112rpx;
  background: var(--color-brand-deep);
}

.wave-bar--9 {
  height: 68rpx;
  background: rgba(93, 190, 136, 0.66);
}

.wave-bar--10 {
  height: 48rpx;
}

.recording-tips {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.recording-tip {
  display: flex;
  align-items: center;
  gap: 12rpx;
  font-size: 26rpx;
  font-weight: 700;
}

.recording-tip--cancel {
  color: #b45f3b;
}

.recording-tip--done {
  color: var(--color-brand-deep);
}

.recording-icon-arrow {
  position: relative;
  width: 32rpx;
  height: 32rpx;
}

.recording-icon-arrow::before {
  content: "";
  position: absolute;
  left: 13rpx;
  top: 7rpx;
  width: 6rpx;
  height: 20rpx;
  border-radius: 999rpx;
  background: #ff8a65;
}

.recording-icon-arrow::after {
  content: "";
  position: absolute;
  left: 7rpx;
  top: 4rpx;
  width: 14rpx;
  height: 14rpx;
  border-left: 5rpx solid #ff8a65;
  border-top: 5rpx solid #ff8a65;
  transform: rotate(45deg);
}

.recording-icon-check {
  position: relative;
  width: 32rpx;
  height: 32rpx;
  border: 4rpx solid var(--color-brand-deep);
  border-radius: 999rpx;
}

.recording-icon-check::after {
  content: "";
  position: absolute;
  left: 8rpx;
  top: 7rpx;
  width: 12rpx;
  height: 7rpx;
  border-left: 4rpx solid var(--color-brand-deep);
  border-bottom: 4rpx solid var(--color-brand-deep);
  transform: rotate(-45deg);
}
