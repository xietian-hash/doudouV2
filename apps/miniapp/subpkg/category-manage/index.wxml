<view class="manage-page">
  <view class="manage-nav">
    <view class="nav-back" bindtap="goBack">‹</view>
    <view class="nav-title">分类管理</view>
    <view class="nav-spacer"></view>
  </view>

  <view class="manage-tabs">
    <view class="manage-tab {{type === 1 ? 'manage-tab--active' : ''}}" data-type="1" bindtap="switchType">支出</view>
    <view class="manage-tab {{type === 2 ? 'manage-tab--active' : ''}}" data-type="2" bindtap="switchType">收入</view>
  </view>

  <scroll-view class="manage-list" scroll-y enhanced show-scrollbar="{{false}}">
    <view class="manage-list-inner">
      <view
        wx:for="{{categories}}"
        wx:key="id"
        class="swipe-row"
        data-index="{{index}}"
        bindtouchstart="onTouchStart"
        bindtouchmove="onTouchMove"
        bindtouchend="onTouchEnd"
        bindtouchcancel="onTouchEnd"
      >
        <view class="swipe-actions">
          <view class="swipe-action swipe-action--edit" data-id="{{item.id}}" catchtap="openEdit">编辑</view>
          <view class="swipe-action swipe-action--delete" data-id="{{item.id}}" data-name="{{item.name}}" catchtap="askDelete">删除</view>
        </view>
        <view class="swipe-content" style="transform: translateX({{item.slideX || 0}}rpx);">
          <view class="manage-card" data-id="{{item.id}}" data-name="{{item.name}}" bindtap="openChildren">
            <view class="card-icon">{{item.icon || '📂'}}</view>
            <view class="card-main">
              <text class="card-title">{{item.name}}</text>
              <text class="card-sub">{{item.childrenCount}}个子分类</text>
            </view>
            <text class="card-arrow">›</text>
          </view>
        </view>
      </view>

      <view wx:if="{{!categories.length}}" class="empty-state">暂无分类</view>
    </view>
  </scroll-view>

  <view class="bottom-add">
    <view class="bottom-add-btn" bindtap="openCreate">
      <text class="bottom-add-plus">+</text>
      <text>添加分类</text>
    </view>
  </view>

  <view wx:if="{{dialogVisible}}" class="dialog-mask">
    <view class="dialog">
      <view class="dialog-head">
        <text class="dialog-title">{{editingId ? '编辑一级分类' : '添加一级分类'}}</text>
        <text class="dialog-close" bindtap="closeDialog">×</text>
      </view>
      <view class="dialog-body">
        <view class="field">
          <text class="field-label">分类名称</text>
          <input class="field-input" value="{{form.name}}" bindinput="onNameInput" placeholder="请输入分类名称" maxlength="4" />
        </view>
        <view class="field">
          <text class="field-label">分类图标</text>
          <input class="field-input" value="{{form.icon}}" bindinput="onIconInput" placeholder="请输入 Emoji，可不填" />
        </view>
      </view>
      <view class="dialog-actions">
        <view class="dialog-btn dialog-btn--secondary" bindtap="closeDialog">取消</view>
        <view class="dialog-btn dialog-btn--primary" bindtap="save">确认</view>
      </view>
    </view>
  </view>

  <view wx:if="{{confirmVisible}}" class="dialog-mask">
    <view class="confirm-dialog">
      <view class="confirm-icon">!</view>
      <text class="confirm-title">删除分类</text>
      <text class="confirm-text">该分类有关联账单或子分类时不可删除。确认删除“{{pendingDeleteName}}”？</text>
      <view class="dialog-actions">
        <view class="dialog-btn dialog-btn--secondary" bindtap="closeConfirm">取消</view>
        <view class="dialog-btn dialog-btn--danger" bindtap="confirmDelete">确认删除</view>
      </view>
    </view>
  </view>
</view>
